const SetConteur = (state=0,action) => {
   
        // if(action.type==='SET_COMPTEUR'){
        
        //     console.log(action.payload)
            
        //     return action.payload.map(e=>e.conteur).reduce((c,v)=>c+v)
        // }
        // else 
        return state
    
}
 
export default SetConteur;